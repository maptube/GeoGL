#pragma once

#include <unordered_map>

#include "gengine.h"

namespace gengine {

//NOT USED

	//This ties up the vertex and index buffers needed to render with the shader uniforms that they're bound to
	//class VertexArrayObject
	//{
	//public:
	//	GLuint _vaoID; //vertex array object id used for binding
	//	std::unordered_map<int,std::string> _Attributes; //this contains a map of the index and name

	//	VertexArrayObject(void);
	//	~VertexArrayObject(void);

	//	void AddVertexAttribute(unsigned int Index, std::string Name); //setup
	//	void Bind(); //bind for use?
	//};

} //namespace gengine
